<%- include('./htmlHeader.ejs') %>

<nav class="navbar navbar-expand-lg bg-custom navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="index.html"><img src="/shared/img/logo300.png" width="54" alt=""> </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
 Menu <span class="navbar-toggler-icon"></span>
</button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">

                <li class="nav-item">
                    <a class="nav-link" href="/auth/signUp">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/logIn">Login</a>
                </li>
            </ul>

        </div>
    </div>
</nav>
<!-- /navbar -->

<div class="container text-center my-5">
    <div class="user my-3">
        <i class="far fa-edit user-icon"></i>
        <h4 class="login">Register</h4>
    </div>
    <div class="card p-5 w-50 m-auto">
        <form method="POST" action="/auth/signUp">
            <input class="form-control" placeholder="Enter your Name" type="text" name="name" value="<%= data?.name %>">
            <% if (vaildationErorr?.includes('name')) { %>
                <p class="text-danger">Please enter your name</p>
            <% } %>
            <input class="form-control my-2 " placeholder="Enter your email" type="email" name="email" value="<%= data?.email %>">
            <% if (vaildationErorr?.includes('email')) { %>
                <p class="text-danger">Invaild Email</p>
            <% } %>
            <input class="form-control  " placeholder="Enter your Password" type="password" name="password" value="<%= data?.password %>">
            <% if (vaildationErorr?.includes('password')) { %>
                <p class="text-danger">password must be 8 characters long and contain at least one lowercase letter,one uppercase letter,numbers,Special_Char</p>
            <% } %>
            <input class="form-control  my-2" placeholder="Password Confirmation" type="password" name="PasswordConfirmation" value="<%= data?.PasswordConfirmation %>">
            <% if (vaildationErorr?.includes('PasswordConfirmation')) { %>
                <p class="text-danger">confirmPassword must be as password</p>
            <% } %>
            <button class="btn btn-default-outline my-4 w-100 rounded">Register</button>
            <% if (error) { %>
                <div class="alert alert-warning" role="alert">
                    <strong><%= error %></strong> 
                </div>
            <% } %>
            <p class="text-center mt-2"> Aready Have Account ? <a style=" text-decoration: none"  href="/auth/logIn">logIn</a></p>
        </form>
    </div>
</div>




<%- include('./htmlFooter.ejs') %>